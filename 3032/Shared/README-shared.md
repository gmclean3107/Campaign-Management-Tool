This project contains any files or dependencies that need to be used across both the client(frontend) project, and the server(backend) project.

This may include:

- Classes
- Data Models
- Dependencies
